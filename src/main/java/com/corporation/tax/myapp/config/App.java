package com.corporation.tax.myapp.config;

/** Hello world! */
public class App {
  /* public static void main(String[] args) {
    BusinessRootFactory autoBusinessFactory = new BusinessRootFactory();
    BusinessRoot rootAuto = autoBusinessFactory.getAutoBusiness(1);
    System.out.println(
        "Auto Business tax : " + rootAuto.getTax(rootAuto.getTypeOfBusiness().getRatio(), 100));

    BusinessRoot rootSAS = autoBusinessFactory.getAutoBusiness(2);
    System.out.println(
        "SAS Business tax : " + rootSAS.getTax(rootAuto.getTypeOfBusiness().getRatio(), 500));
  }*/
}
